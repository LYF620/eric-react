import { createElement, createTextElement } from './CreateElement'
import { render, useState } from './Render'
const Eric = {
  createElement,
  createTextElement,
  render,
  useState,
}

export { Eric }
